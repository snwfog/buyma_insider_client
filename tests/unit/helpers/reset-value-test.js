
import { resetValue } from 'buyma-insider-client/helpers/reset-value';
import { module, test } from 'qunit';

module('Unit | Helper | reset value');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = resetValue([42]);
  assert.ok(result);
});

