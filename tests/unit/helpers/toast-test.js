
import { toast } from 'buyma-insider-client/helpers/toast';
import { module, test } from 'qunit';

module('Unit | Helper | toast');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = toast([42]);
  assert.ok(result);
});

