
import { disable } from 'buyma-insider-client/helpers/disable';
import { module, test } from 'qunit';

module('Unit | Helper | disable');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = disable([42]);
  assert.ok(result);
});

