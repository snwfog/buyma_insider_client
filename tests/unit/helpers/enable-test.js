
import { enable } from 'buyma-insider-client/helpers/enable';
import { module, test } from 'qunit';

module('Unit | Helper | enable');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = enable([42]);
  assert.ok(result);
});

