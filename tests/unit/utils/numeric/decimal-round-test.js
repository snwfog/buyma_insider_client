import numericDecimalRound from 'buyma-insider-client/utils/numeric/decimal-round';
import { module, test } from 'qunit';

module('Unit | Utility | numeric/decimal round');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = numericDecimalRound();
  assert.ok(result);
});
