# BUYMA バイマ INSIDER

## Note
- Adding ember-cli-app-version increased build time by 10s ish

TODO:
- Add ember cp validation
- Add ember features
- Quick currency calculator, next to every article, and on the main page
- Search history / View history
- Improve the loading bar, to use page loaded event to move forward the bar

## Deployment
- em deploy <deployTarget> (staging, production) --verbose
- em deploy:activate <deployTarget> (staging, production) --revision <sha>
- em deploy:list <deployTarget> (staging, production)
